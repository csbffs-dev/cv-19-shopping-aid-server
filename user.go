package main

import (
	"fmt"
	"net/http"
)

// SetupUser sets up a user in storage.
func SetupUser(r *http.Request) (int, error) {
	return http.StatusNotImplemented, fmt.Errorf("not supported yet")
}
